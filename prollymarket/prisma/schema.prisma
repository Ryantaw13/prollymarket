generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://postgres:YmJ7K067rj1PIRl0@db.khtddglxdmgenyfuqvyk.supabase.co:5432/postgres"
}

model User {
  id            Int        @id @default(autoincrement())
  username      String     @unique
  email         String     @unique
  passwordHash  String
  displayName  String?
  balance      Float      @default(1000)
  streakDays   Int        @default(0)
  lastLogin    DateTime?
  approved    Boolean    @default(false)
  achievements String    @default("[]")
  createdAt    DateTime   @default(now())
}

model Market {
  id           Int        @id @default(autoincrement())
  question     String
  description  String?
  category     String     @default("general")
  imageUrl     String?
  creatorId    Int
  yesPrice    Float      @default(0.5)
  noPrice     Float      @default(0.5)
  volume      Int        @default(0)
  outcome     String?
  commentCount Int      @default(0)
  isResolved  Boolean   @default(false)
  createdAt   DateTime  @default(now())
}

model Bet {
  id        Int      @id @default(autoincrement())
  userId   Int
  marketId Int
  amount   Float
  outcome  String
  price    Float
  payout   Float?
  realized Boolean  @default(false)
  createdAt DateTime @default(now())
}

model Comment {
  id        Int      @id @default(autoincrement())
  userId    Int
  marketId  Int
  content   String
  createdAt DateTime @default(now())
}